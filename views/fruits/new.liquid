{% layout 'layout.liquid' %}
{% block content %}

  <div class="container-sm">
    <form action="/cars" method="POST">
      <fieldset>
        <legend>Create New Fruit</legend>
        <label class="form-label">Name:
        </label>
        <input class="form-control" type="text" name="name" placeholder="enter the name of the car">
        <label class="form-label">Color:
        </label>
        <input class="form-control" type="text" name="color" placeholder="what color is the car?">
        <label class="form-label">Ready To Drive:
        </label>
        <input class="form-check-input" type="checkbox" name="readyToDrive">
      </fieldset>
      <input class="btn btn-success" type="submit" value="Create New Car">
    </form>
  </div>

{% endblock %}}}}}}